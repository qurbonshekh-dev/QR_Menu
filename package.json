{
  "name": "food",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --workspace apps/guest run dev",
    "build": "npm --workspace apps/guest run build",
    "typecheck": "npm --workspace apps/guest run typecheck",
    "lint": "oxlint apps packages"
  },
  "devDependencies": {
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2"
  }
}

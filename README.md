# QR Menu

Гостевое QR-меню ресторана: сканируешь код на столе — выбираешь блюда — оформляешь заказ.
Mobile-first веб-приложение на React + Vite + TypeScript.

**Живая версия:** https://qr-menu-pi-sage.vercel.app

Особенность проекта — дизайн-система существует одновременно в коде и в Figma,
компонент к компоненту, токен к токену.

## Запуск

```bash
npm --prefix app install
npm --prefix app run dev
```

Дев-сервер слушает и по локальной сети, поэтому приложение можно открыть на телефоне:
Vite напечатает адрес вида `http://192.168.x.x:5173` — телефон должен быть в той же Wi-Fi.
Правки подхватываются на лету.

## Стол из QR-кода

Номер стола приходит параметром: `/#/?table=7`. Без параметра показывается стол 12.

## Деплой

`git push origin main` — Vercel пересобирает и обновляет прод автоматически.
В настройках проекта Vercel **Root Directory = `app`**.

## Где что лежит

| Путь | Что |
|---|---|
| `app/src/components/` | Дизайн-система: atoms / molecules / organisms |
| `app/src/pages/` | Экраны приложения |
| `app/src/data/` | Типы, мок-меню, слой доступа к данным |
| `app/src/tokens/` | Токены: цвета, отступы, радиусы, типографика |
| `artifacts/` | Токены-источник, карта id Figma, отчёт о parity |
| `directives/` | Инструкции пайплайна синхронизации с Figma |
| `docs/tz.md` | Техническое задание на продукт |
| `CLAUDE.md` | Полный контекст проекта для ИИ-ассистента |
